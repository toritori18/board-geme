現状、このプロジェクトには自動フォーマッタ（Prettier等）が未導入です。スタイルの一部は ESLint（`next/core-web-vitals` + `@typescript-eslint`、[.eslintrc.json](../../.eslintrc.json)）でチェックされますが、フォーマット専用ツールの導入可否は未検討です。

実行せず、その旨をユーザーに報告してください。導入を希望する場合は Prettier 等の候補をユーザーに確認してから追加してください。
